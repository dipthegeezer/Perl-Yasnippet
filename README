Download and install yasnippet
copy snippets into the directory where
snippets get loaded from.
copy perl-yasnippet to /.emacs.d/plugins/yasnippet-xx
then add (require 'perl-yasnippet) to your .emacs config
file.

